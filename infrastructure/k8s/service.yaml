apiVersion: v1
kind: Service
metadata:
  name: research-api-service
  namespace: research-platform
  labels:
    app: research-api
spec:
  type: ClusterIP
  selector:
    app: research-api
  ports:
    - port: 8000
      targetPort: 8000
      name: http
